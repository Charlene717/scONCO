% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_cancer_marker_db.R
\name{load_cancer_marker_db}
\alias{load_cancer_marker_db}
\title{Load the pan-cancer marker database (optionally a single cancer-type module)}
\usage{
load_cancer_marker_db(
  version = "v1.0",
  organism = c("human", "mouse"),
  module = "pan"
)
}
\arguments{
\item{version}{Character. One of \code{"v1.0"} (default), or any of
\code{"v_AI_ChatGPT5_DeepResearch"}, \code{"v_AI_ChatGPT5_Pro"},
\code{"v_AI_ChatGPT5_Thinking"}, \code{"v_AI_ChatGPT5_Instant"},
\code{"v_AI_Gemini"}, \code{"v_Expert"} to load a single source.}

\item{organism}{\code{"human"} (default) or \code{"mouse"}.}

\item{module}{Optional character. \code{"pan"} (default; everything) or one of the
cancer-type modules: \code{"melanoma"}, \code{"LUAD"}, \code{"LUSC"}, \code{"BRCA"}, \code{"HCC"},
\code{"CRC"}, \code{"PDAC"}, \code{"STAD"}, \code{"OV"}, \code{"PRAD"}, \code{"GBM"}, \code{"RCC"}, \code{"HNSC"},
\code{"BLCA"}. When a specific module is given, malignant cell types from the
other cancers are dropped, but the generic malignant programmes and all
TME / normal compartments are kept.}
}
\value{
A data.frame with columns:
\itemize{
\item \code{gene} — HUGO symbol (or MGI for mouse)
\item \code{Level1} … \code{Level4_Abb} — hierarchical labels
\item \code{sign} — \code{"positive"} (default) or \code{"negative"}, where annotated
\item \code{ai_support} — fraction of AI sources agreeing (0–1)
\item \code{lit_support} — literature support score (0–1)
\item \code{specificity_index} — Wilcoxon AUC on a pan-cancer reference (0–1) if computed
}
}
\description{
Loads the curated pan-cancer marker database shipped with scONCO. By default
it returns the full pan-cancer \code{markers_df} (malignant + immune + stromal +
endothelial + normal-epithelial compartments). A \code{module} can be supplied to
restrict the malignant compartment to a single cancer type while keeping the
shared tumour-microenvironment (TME) cell types.
}
\examples{
\dontrun{
  m   <- load_cancer_marker_db()                       # full pan-cancer, human
  mm  <- load_cancer_marker_db(organism = "mouse")     # mouse pan-cancer
  mel <- load_cancer_marker_db(module = "melanoma")    # melanoma + TME only
}

}
