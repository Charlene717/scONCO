% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_scONCO.R
\name{run_scONCO}
\alias{run_scONCO}
\title{Run the scONCO hierarchical annotation pipeline}
\usage{
run_scONCO(
  seurat_obj,
  markers_df,
  cluster_col = "seurat_clusters",
  hierarchy = c("Level1", "Level2", "Level3", "Level4_Abb"),
  weights = c(alpha = 0.6, beta = 0.2, gamma = 0.2, delta = 0.2),
  apply_confidence = FALSE,
  apply_cnv_gate = FALSE,
  cnv_method = "precomputed",
  cnv_col = NULL,
  cnv_score = NULL,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object that has been QC'd, normalised, scaled,
PCA-projected, and clustered (so that \code{meta.data[[cluster_col]]} exists).}

\item{markers_df}{Marker database in long format with columns
\code{gene}, \code{Level1}, \code{Level2}, \code{Level3}, \code{Level4_Abb}, optionally \code{sign},
\code{ai_support}, \code{lit_support}, \code{specificity_index}.}

\item{cluster_col}{Name of the cluster column in \code{meta.data}. Default
\code{"seurat_clusters"}.}

\item{hierarchy}{Character vector of column names in \code{markers_df} defining
the hierarchy from coarse to fine. Default
\code{c("Level1", "Level2", "Level3", "Level4_Abb")}.}

\item{weights}{Named numeric vector of scoring weights:
\code{c(alpha = 0.6, beta = 0.2, gamma = 0.2, delta = 0.2)}.}

\item{apply_confidence}{Logical. If \code{TRUE}, also runs \code{apply_hierarchical_reject()}
and \code{apply_conformal_to_seurat()} with default parameters.}

\item{apply_cnv_gate}{Logical. If \code{TRUE}, also runs the CNV-aware malignant
gate (\code{\link[=apply_cnv_malignant_gate]{apply_cnv_malignant_gate()}}), adding \code{scONCO_malignant_call},
\code{scONCO_malignant_prob}, \code{scONCO_cnv_score}, \code{scONCO_L1_gated},
\code{scONCO_L4_gated}, \code{scONCO_gate_corrected}.}

\item{cnv_method, cnv_col, cnv_score}{Passed to the CNV gate. With
\code{cnv_method = "precomputed"} (default) supply \code{cnv_col} (a meta.data column
holding a CopyKAT/scATOMIC/inferCNV call) or a precomputed \code{cnv_score}.}

\item{verbose}{Whether to print progress.}
}
\value{
Seurat object with extra meta.data columns:
\code{scONCO_L1} ... \code{scONCO_L4} (or whatever \code{hierarchy} names),
and (if \code{apply_confidence}) \verb{scONCO_L\{k\}_confidence},
\verb{scONCO_L\{k\}_status}, \code{conformal_set}, \code{conformal_set_size}.
}
\description{
Top-level entry point. Annotates a pre-clustered Seurat object using the
4-level pan-cancer marker database (Level1 -> Level4_Abb) and writes one
meta.data column per level.
}
\examples{
\dontrun{
  library(Seurat); library(scONCO)
  markers_df <- load_cancer_marker_db()
  seu <- run_scONCO(seu, markers_df)
  table(seu$scONCO_L4)
}

}
