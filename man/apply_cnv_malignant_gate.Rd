% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cancer_layer.R
\name{apply_cnv_malignant_gate}
\alias{apply_cnv_malignant_gate}
\title{Apply the CNV-aware malignant gate (A3)}
\usage{
apply_cnv_malignant_gate(
  seurat_obj,
  cnv_score = NULL,
  method = "precomputed",
  cnv_col = NULL,
  weights = c(marker = 0.4, cnv = 0.45, module = 0.15),
  fusion = c("weighted", "logistic"),
  threshold = 0.5,
  uncertain_margin = 0.05,
  restrict_to_epithelial = TRUE,
  correct_labels = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{A Seurat object previously annotated by \code{\link[=run_scONCO]{run_scONCO()}}.}

\item{cnv_score}{Optional named numeric vector (cell -> \verb{[0,1]}). If \code{NULL},
computed via \code{\link[=compute_cnv_score]{compute_cnv_score()}}.}

\item{method, cnv_col}{Passed to \code{\link[=compute_cnv_score]{compute_cnv_score()}} when \code{cnv_score} is \code{NULL}.}

\item{weights}{Named numeric \code{c(marker, cnv, module)} (auto-normalised).}

\item{fusion}{\code{"weighted"} or \code{"logistic"}.}

\item{threshold}{Decision threshold on \code{P(malignant)} (default 0.5).}

\item{uncertain_margin}{Half-width of the Uncertain band (0 disables).}

\item{restrict_to_epithelial}{Gate only epithelial-lineage cells (default TRUE).}

\item{correct_labels}{Reconcile marker label with the gate (default TRUE).}

\item{verbose}{Logical.}
}
\value{
Updated Seurat object.
}
\description{
Fuses scONCO's marker-based malignant evidence, a per-cell CNV score, and the
tumour-type module prior into a per-cell malignant probability
\code{P(malignant) = sigma(w_marker*marker + w_cnv*CNV + w_module*module)}, then
classifies each (epithelial-lineage) cell as Malignant / Non_malignant /
Uncertain and, optionally, reconciles the marker label with the gate. This is
scONCO's answer to the central tumour-scRNA-seq problem of distinguishing
malignant cells from their normal cell-of-origin.
}
\details{
Adds to \code{meta.data}: \code{scONCO_cnv_score}, \code{scONCO_malignant_prob},
\code{scONCO_malignant_call}, \code{scONCO_L1_gated}, \code{scONCO_L4_gated},
\code{scONCO_gate_corrected}.
}
